syntax = "proto3";

package io.scalac.newspaper;

message ChangeDetected {
    string pageUrl = 1;
    string pageContent = 2;
}

message ContentFetched {
    string pageUrl = 1;
    string pageContent = 2;
}
